/* eslint-disable no-unused-vars */

export interface ElArr {
  el: Element;
  i: number;
}

// eslint-disable-next-line no-shadow
export enum States {
  X = 'X',
  O = 'O',
  D = 'D',
  NA = 'NA',
  INVALID = 'INVALID'
}

// eslint-disable-next-line no-shadow
export enum PlayerStates {
  X = 'X',
  O = 'O'
}
